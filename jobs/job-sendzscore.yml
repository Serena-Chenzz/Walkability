apiVersion: batch/v1
kind: Job
metadata:
  name: process-item-sendzscore
  labels:
    jobgroup: jobwalkability
spec:
  template:
    metadata:
      name: jobwalkability
      labels:
        jobgroup: jobwalkability
    spec:
      containers:
        - name: sendzscore
          image: serenalovedavid/walkability:sendzscore
          command: ["sh", "-c", "echo Processing item SendZscore"]
          volumeMounts:
            - mountPath: /app/src/main/java/org/mccaughey/output/
              name: output-file
            - mountPath: /root/.m2
              name: m2
      restartPolicy: OnFailure
      volumes:
        - name: output-file
          hostPath:
            path: /mnt/sharedfolder/output/
        - name: m2
          hostPath:
            path: /home/ubuntu/.m2